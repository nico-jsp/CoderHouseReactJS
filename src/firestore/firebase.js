import { doc, getDoc, getFirestore } from "firebase/firestore";

//Funciones a agregar
// buscarItem(id)
// buscarCategoria(categoryId)
// modificarItems(id)
// agretarItem(item)



useEffect(() => {
  const db = getFirestore();

  const items = doc(db, "items", "msEJujw8qafYvy1ebuzG")
  return () => {
    second
  }
}, [third])
